--- a/dmenu.c	2015-05-14 12:39:02.707106324 -0400
+++ b/dmenu.c	2015-05-14 12:40:30.528207851 -0400
@@ -302,6 +302,15 @@
 	default:
 		if(!iscntrl(*buf))
 			insert(buf, len);
+		else if(ev->state & ShiftMask)
+		switch(ksym) {
+		case XK_Insert:
+			XConvertSelection(dc->dpy, XA_PRIMARY,
+			                  utf8, utf8, win, CurrentTime);
+			return;
+		default:
+			return;
+		}
 		break;
 	case XK_Delete:
 		if(text[cursor] == '\0')
